{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.change_email'|trans}}
{% endblock %}

{% block body %}
    <h1>{{ 'title.change_email'|trans}}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}